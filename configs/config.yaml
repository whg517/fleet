server:
  port: 8080
  read_timeout: 30s
  write_timeout: 30s
  allowed_origins:
    - http://localhost:3000

database:
  host: localhost
  port: 5432
  user: fleet
  password: ""  # Set via FLEET_DATABASE_PASSWORD
  dbname: fleet
  sslmode: disable
  max_open_conns: 25
  max_idle_conns: 10

redis:
  addr: localhost:6379
  password: ""
  db: 0

log:
  level: info
  encoding: json  # json 或 console
