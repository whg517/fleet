run:
  timeout: 5m
  go: "1.24"

linters:
  enable:
    - gofmt
    - goimports
    - govet
    - ineffassign
    - staticcheck
    - unused
    - errcheck

linters-settings:
  goimports:
    local-prefixes: github.com/whg517/fleet
  errcheck:
    check-type-assertions: true
