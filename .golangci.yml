version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - goimports
    - gofmt
  exclusions:
    presets:
      - common-false-positives
      - legacy
      - std-error-handling
